export const API_ENDPOINT = (process.env.VITE_API_ENDPOINT ?? '') as string;
export const APP_ENV = (process.env.VITE_APP_ENV ?? 'local') as string;
export const APP_URL = (process.env.VITE_APP_URL ?? '') as string;
export const APP_TITLE = (process.env.VITE_APP_TITLE ?? 'App') as string;
